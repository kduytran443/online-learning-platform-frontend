import { Box } from "@mui/material";

export function AboutView() {
    return <Box
        sx={{ display: 'flex' }}
    >
      <Box component="main" sx={{ flexGrow: 1, p: 4 }}>
        Dashboard content for AboutView
      </Box>
    </Box>;
}
